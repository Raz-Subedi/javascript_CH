

    // Returning the undefined value.

    var sum = 0;
    function addThree(){
        sum = sum +3;
    }

    /*
     Functions can have the return statements, but they don't have to.
     In above case the function adds 3 to the sum variable.
     It doesn,t return anything.
     So, if you don't specify a return value, the return value is just undefined.
    */

    function addFive(){
        sum += 5;
    }
